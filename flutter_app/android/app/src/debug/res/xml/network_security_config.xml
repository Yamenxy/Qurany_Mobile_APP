<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug/profile builds: same local dev hosts as release, plus LAN IPs for
  physical-device testing against a backend on your PC (same Wi‑Fi).
  Add your machine's LAN address here if it differs from the example below.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Example LAN IP used for local backend testing; adjust as needed -->
        <domain includeSubdomains="false">192.168.1.25</domain>
    </domain-config>
</network-security-config>
