name: Build iOS IPA

on:
  push:
    branches: [main]
  workflow_dispatch:  # Manual trigger

jobs:
  build-ios:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: stable
          cache: true
      
      - name: Flutter doctor
        run: flutter doctor -v
      
      - name: Get dependencies
        working-directory: flutter_app
        run: flutter pub get
      
      - name: Install CocoaPods
        working-directory: flutter_app/ios
        run: pod install || pod install --repo-update
      
      # ─── Option A: Build WITHOUT code signing (produces .app, not installable on device) ───
      - name: Build iOS (no codesign)
        working-directory: flutter_app
        run: flutter build ios --release --no-codesign
      
      # ─── Option B: Build WITH code signing (uncomment this and comment Option A) ───
      # You need to add these secrets in GitHub repo Settings > Secrets:
      #   - APPLE_CERTIFICATE_BASE64: base64 encoded .p12 certificate
      #   - APPLE_CERTIFICATE_PASSWORD: password for the .p12
      #   - APPLE_PROVISIONING_PROFILE_BASE64: base64 encoded .mobileprovision
      #   - KEYCHAIN_PASSWORD: any random password for temp keychain
      #
      # - name: Install Apple Certificate
      #   env:
      #     CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
      #     CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      #     KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
      #   run: |
      #     # Create variables
      #     CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
      #     KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
      #     
      #     # Import certificate from secrets
      #     echo -n "$CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
      #     
      #     # Create temporary keychain
      #     security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
      #     security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
      #     security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
      #     
      #     # Import certificate to keychain
      #     security import $CERTIFICATE_PATH -P "$CERTIFICATE_PASSWORD" \
      #       -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
      #     security set-key-partition-list -S apple-tool:,apple: \
      #       -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
      #     security list-keychain -d user -s $KEYCHAIN_PATH
      #
      # - name: Install Provisioning Profile
      #   env:
      #     PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
      #   run: |
      #     PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
      #     echo -n "$PROVISIONING_PROFILE_BASE64" | base64 --decode -o $PP_PATH
      #     mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
      #     cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles
      #
      # - name: Build IPA
      #   working-directory: flutter_app
      #   run: |
      #     flutter build ipa --release \
      #       --export-options-plist=ios/ExportOptions.plist
      #
      # - name: Upload IPA
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: qurany-ipa
      #     path: flutter_app/build/ios/ipa/*.ipa
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: qurany-ios-build
          path: flutter_app/build/ios/iphoneos/
          retention-days: 14
